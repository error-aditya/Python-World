# unordered collection
# element must be unique 
# set is mutable but its values must be immutable
# ignore duplicate values

num = {1,2,3,4,5}
print(num,'\n',type(num))

# it will remove the duplicate values however it won't give error
nums = {1,2,1,2,3,4,5} 
print(len(nums))

# create empty set
empt = set()